package com.rehan.starwarsblastertournament.domain.model

// Whether a player won, lost, or drew a particular match. Used to color-code the
// Player Matches screen (green/red/white).
enum class MatchResult {
    WIN,
    LOSS,
    DRAW
}